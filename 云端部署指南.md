# 云端部署指南（零基础手把手版）

> 目标：把你这台电脑上的「极简业务工作台」搬到网上，得到一个**网址**。
> 之后**电脑浏览器、手机浏览器、手机桌面图标（App）、安卓安装包**全部访问同一个网址，
> 计划、客户、账单、锻炼数据互通，连**网易云音乐、哔哩哔哩**在手机上也能直接用。
>
> 你**不需要懂任何代码**，全程用鼠标点就行。下面每一步都写明了「你点哪里、填什么」。

---

## 一、你会得到什么

部署成功后，你拥有：

| 设备 | 怎么用 | 能用什么 |
|---|---|---|
| 电脑 | 浏览器打开你的网址 | 全部功能（含音乐/B站） |
| 手机浏览器 | 打开同一个网址 | 全部功能（含音乐/B站） |
| 手机桌面 | 浏览器「添加到主屏幕」→ 像 App 一样点开 | 全部功能 |
| 安卓手机 | 安装打包好的 APK（可选） | 全部功能 |
| 数据互通 | 任意设备登录同一个同步账号 | 各设备数据自动同步 |

核心原理一句话：**我们把 `server.js`（它既负责显示网页、又负责代理网易云/B站接口）部署到云端服务器 Render。手机和电脑访问的是同一个云端网址，所以一切都同源、直接可用，不用再改任何代码。**

---

## 二、需要准备的东西（都免费）

1. 一个 **GitHub** 账号（用来存放代码，相当于一个网盘）—— https://github.com
2. 一个 **Render** 账号（用来把代码变成网上运行的网址）—— https://render.com
   - Render 注册时可以选择「用 GitHub 账号登录」，一步搞定，不用单独记密码。
3. 一个 **Supabase** 账号（负责数据多设备同步，免费）—— https://supabase.com
   - 这部分在「第五步」才用到，先不急。

> 提示：GitHub 和 Render 都需要能收验证码的邮箱。国内邮箱（QQ/163）一般可用。

---

## 三、第一步：把文件传到 GitHub（像传网盘一样）

Render 需要从一个代码仓库读取你的文件。我们用工蜂/网盘的网页上传方式，**不用装任何软件、不用输命令**。

### 1. 注册并登录 GitHub
- 打开 https://github.com ，点右上角 **Sign up** 注册；已有账号就 **Sign in** 登录。

### 2. 新建一个仓库（Repository = 一个文件夹）
- 登录后，点页面右上角你的头像左边那个 **「＋」** → 选 **New repository**。
- 在新建页面填：
  - **Repository name**（仓库名）：随便起，比如 `workbench`（只能英文/数字/横杠）
  - **Description**：可留空
  - 选 **Public**（公开，免费必须选公开；代码不含你的隐私数据，只有程序）
  - 下面 **Add a README file** 不用勾
- 点绿色按钮 **Create repository**。

### 3. 把工作台文件拖进去
- 进到刚建好的仓库页面，你会看到一行提示「…or create the first commit」。
- 点中间的 **「uploading an existing file」** 链接（或右上角 **Add file → Upload files**）。
- 把你电脑里 `极简业务工作台` 文件夹里的**这些文件**全部选中、拖到网页的虚线框里：

  ```
  必传（少了任何一个都会出错）：
  ✅ 极简业务工作台.html
  ✅ server.js
  ✅ sync-config.js
  ✅ package.json
  ✅ manifest.webmanifest
  ✅ sw.js
  ✅ icon.svg
  ✅ supabase.sql
  可选（不影响运行，建议一起传）：
  ✅ api/            （文件夹，里面是 index.js）
  ✅ vercel.json
  ✅ render.yaml
  ✅ electron-main.js
  ✅ capacitor.config.json
  ✅ README.md
  ```

  > 小技巧：可以直接把整个文件夹窗口里的文件**一次性框选拖入**；`api` 是子文件夹，单独再拖一次即可。

- 拖完后，页面下方 **Commit changes** 区域：
  - 第一行随便写，比如 `first upload`
  - 点绿色 **Commit changes** 按钮，等进度条走完。

✅ 到这里，你的代码已经在 GitHub 上了。

---

## 四、第二步：在 Render 一键部署成网址

### 1. 注册并登录 Render
- 打开 https://render.com ，点 **Sign Up**，建议选 **「Continue with GitHub」**（用 GitHub 登录，自动授权）。
- 授权时如果问「哪些仓库」，选 **All repositories**（全部）或只选你刚建的 `workbench`。

### 2. 新建 Web Service
- 登录后，点右上角 **「＋ New」** → 选 **Web Service**。

### 3. 连接 GitHub 仓库
- 在 "Connect a repository" 区域，搜索或找到你刚建的仓库 `workbench`，点它的 **Connect**。

### 4. 填写部署参数（照抄下面的就行）

| 选项 | 填什么 | 说明 |
|---|---|---|
| **Name** | `workbench`（随意） | 服务名字 |
| **Region** | **Singapore**（新加坡） | 离国内近、速度快；没有就选 Oregon |
| **Branch** | `main`（默认） | 一般用默认的 |
| **Root Directory** | 留空 | 代码在仓库根目录 |
| **Runtime** | **Node** | 自动识别，确认是 Node |
| **Build Command** | `npm install --omit=dev` | ⚠️ 必须填这个（不装打包用的 Electron，省时） |
| **Start Command** | `node server.js` | ⚠️ 必须填这个 |
| **Plan** | **Free** | 免费档，够个人用 |

- 其余选项保持默认。
- 点最下方的蓝色 **Create Web Service**。

### 5. 等待部署（约 1～3 分钟）
- 页面会显示黑色日志在滚动，状态从 `Build in progress` → `Live`。
- 看到绿色 **Live** 就成功了！
- 页面顶部会显示你的网址，形如：
  ```
  https://workbench.onrender.com
  ```
  （`workbench` 是你起的名字，每个人不同）

✅ **记下这个网址**。它就是你的云端工作台地址。

> ⚠️ 免费档注意：Render 在「15 分钟没人访问」后会自动休眠以省资源。
> 休眠后**第一次**打开会慢 30～60 秒（在唤醒服务器），之后就快了。
> 这是正常现象，不是坏了。

---

## 五、第三步：马上使用（电脑 + 手机）

### 电脑
- 浏览器打开你的网址 `https://workbench.onrender.com`
- 看到的界面和之前本地版一模一样。
- 网易云音乐、B站功能**直接可用**（云端已代理，无需再开 bat）。

### 手机（最推荐的方式：PWA，像 App 一样）
以**手机自带浏览器**为例（安卓 Chrome / 苹果 Safari 都行）：

1. 手机浏览器打开同一个网址 `https://workbench.onrender.com`
2. 安卓 Chrome：点右上角 **「⋮」菜单 → 添加到主屏幕**
   苹果 Safari：点底部 **「分享 □↑」→ 添加到主屏幕**
3. 桌面出现一个「极简业务工作台」图标，点它就和 App 一样全屏打开。
4. 音乐、B站、数据同步全部可用。

### 数据互通（多设备同步）
要让手机和电脑数据一致，需要一个同步账号（Supabase，免费）：

1. 打开 https://supabase.com 注册登录 → **New Project**（新建项目）→ 等它初始化。
2. 左侧菜单 **SQL Editor** → **New query** → 把本仓库里的 `supabase.sql` 文件内容**整段复制粘贴**进去 → 点 **Run**（执行）。
3. 左侧 **Project Settings → API**：
   - 复制 **Project URL**（形如 `https://xxxx.supabase.co`）
   - 复制 **anon public key**（一长串）
4. 打开你电脑上的 `sync-config.js` 文件（用记事本打开），把这两段值填进去：
   ```js
   SUPABASE_URL:      'https://你复制的URL',
   SUPABASE_ANON_KEY: '你复制的key',
   ```
   保存文件。
5. 在 GitHub 里更新这个文件（重复第三步的上传动作，覆盖同名文件即可）；Render 会自动重新部署（约 1 分钟）。
6. 重新打开工作台网址 → 左侧「数据·设置」→ **☁️ 云端同步** → 用**同一个邮箱+密码**在手机和电脑分别登录。
7. 之后任意一端改动，另一端自动同步。

> 更详细的 Supabase 图文步骤见 `README.md`。

---

## 六、第四步（可选）：打包安卓 APK 安装包

只有你想生成「安卓安装包文件」时才做。需要在你**自己的 Windows 电脑**上执行几条命令（本沙箱环境无法打包，你拿回去在自己电脑跑）。

前置（一次性安装，约十几分钟，需联网）：
- 安装 [Android Studio](https://developer.android.com/studio)（自带 SDK）
- 安装 [Node.js](https://nodejs.org)（LTS 版）

步骤：
1. 把云端部署好的整个文件夹下载/同步到你电脑（含 `capacitor.config.json`）。
2. 用记事本打开 `capacitor.config.json`，把那行网址改成你的真实云端地址：
   ```json
   "url": "https://workbench.onrender.com"
   ```
3. 在文件夹里打开命令行（地址栏输入 `cmd` 回车），依次执行：
   ```bash
   npm install
   npx cap add android
   npx cap sync
   npx cap build android
   ```
4. 生成的 APK 在 `android/app/build/outputs/apk/debug/` 里，传到手机安装即可。
   APK 打开直接加载你的云端网址，音乐/B站/同步全可用。

> 嫌命令麻烦也可以：直接用第五步的「手机浏览器添加到主屏幕」，效果几乎一样，还不用打包。

---

## 七、第五步（可选）：打包电脑 EXE 安装包

同样在你**自己的 Windows 电脑**执行：
```bash
npm install
npm run dist:win
```
生成的安装包在 `dist/极简业务工作台-1.0.0-setup.exe`。
双击安装后，EXE 内部已经自带代理服务，网易云/B站 直接可用，且能填云端同步地址实现多端互通。

---

## 八、重要注意事项（一定要看）

1. **免费档会休眠**：15 分钟没人访问，Render 会睡。第一次打开慢一点是正常的，稍等即可。
2. **音乐可能偏慢**：网易云音频是从云端服务器（新加坡/美国）拉取再转发，比本机稍慢属正常；若卡顿明显，可升级 Render 付费档，或用电脑端的 EXE（本地直连更快）。
3. **数据在你自己手里**：业务数据存在浏览器（或 Supabase 云端库，归你自己的项目所有），没有上传给任何第三方。请偶尔在「数据·设置」点一下**导出备份**。
4. **B站登录态在云端**：扫码登录后 Cookie 存在云端服务器文件里，只属于你的服务实例，安全可控；不用了可在 B站模块点「退出登录」。
5. **关于 Vercel（备选平台）**：如果你更习惯 Vercel，本工程也支持——把代码连到 Vercel、Install Command 填 `npm install --omit=dev` 即可。但注意：**Vercel 的函数有 60 秒超时和流量限制，网易云音乐播放可能失败**；数据同步和 B站登录可用。完整音乐体验仍建议用 Render。

---

## 九、常见问题

**Q：打开网址显示 404 / 空白？**
A：确认上传的文件里包含 `server.js` 和 `极简业务工作台.html`；Build Command 和 Start Command 是否照第四步填对；看 Render 日志有没有红色报错。

**Q：音乐放不出来？**
A：云端首次唤醒时会慢，多等几秒；若长期不行，看 Render 日志是否有 `上游错误`，多半是网络拉取慢，可换 Render 付费档或本地 EXE。

**Q：手机和电脑数据不同步？**
A：确认两端都登录了**同一个** Supabase 同步账号，且 `sync-config.js` 已填好密钥并重新部署。

**Q：想换网址/改名字？**
A：在 Render 服务设置里可改名称（会影响网址）；或删掉重建一个。

**Q：完全不会 GitHub 上传怎么办？**
A：可以让会电脑的朋友帮你把文件夹传到 GitHub 一次；之后所有操作（Render 部署、使用）你都能自己点。或者直接用「手机浏览器 + 添加到主屏幕」，连部署都不用管。

---

## 十、一句话总结

> **GitHub 传文件 → Render 连仓库填两行命令 → 得到网址 → 手机电脑都访问它 → 填 Supabase 实现同步。**
> 全程鼠标操作，无需写代码。
